import React,{useEffect,useRef,useState}from'react';import{createRoot}from'react-dom/client';import{Mic,Send,Volume2,ArrowRight,Lightbulb,Square}from'lucide-react';import'./style.css';
const PREVIEW='/api/avatar-preview';
const fetchJson=async(url,options={},timeout=50000)=>{const c=new AbortController(),t=setTimeout(()=>c.abort(),timeout);try{const r=await fetch(url,{...options,signal:c.signal});const d=await r.json();if(!r.ok)throw Error(d.error||`request_failed_${r.status}`);return d}finally{clearTimeout(t)}};
function App(){const[messages,setMessages]=useState([{role:'mike',text:"What's up? I'm Mike. Tell me what you're trying to figure out. We'll figure it out."}]),[input,setInput]=useState(''),[busy,setBusy]=useState(false),[speaking,setSpeaking]=useState(false),[listening,setListening]=useState(false),[video,setVideo]=useState(null),[previewFailed,setPreviewFailed]=useState(false),[avatarReady,setAvatarReady]=useState(false),[error,setError]=useState(''),audioRef=useRef(null),recognitionRef=useRef(null),avatarJobRef=useRef(0);
const stopMike=()=>{avatarJobRef.current++;audioRef.current?.pause();audioRef.current=null;speechSynthesis.cancel();setSpeaking(false);setVideo(null);setAvatarReady(false)};
const generateAvatar=async audioBase64=>{const job=++avatarJobRef.current;try{const ad=await fetchJson('/api/avatar',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({audioBase64})},45000);if(!ad.generationId||job!==avatarJobRef.current)return;for(let i=0;i<48;i++){await new Promise(x=>setTimeout(x,750));if(job!==avatarJobRef.current)return;const sd=await fetchJson('/api/avatar/'+encodeURIComponent(ad.generationId),{},10000);if(job!==avatarJobRef.current)return;if(sd.status==='completed'&&sd.videoUrl){setVideo(sd.videoUrl);setAvatarReady(true);return}if(sd.status==='failed')return}}catch(e){if(e.name!=='AbortError')console.warn('avatar unavailable',e)}};
const speak=async text=>{stopMike();setSpeaking(true);setError('');try{const d=await fetchJson('/api/tts',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({text})},45000);const a=new Audio('data:audio/mpeg;base64,'+d.audioBase64);audioRef.current=a;a.onended=()=>{setSpeaking(false);setVideo(null);setAvatarReady(false)};generateAvatar(d.audioBase64);await a.play()}catch(e){const u=new SpeechSynthesisUtterance(text);u.lang='en-US';u.rate=1.06;u.pitch=.92;u.onend=()=>{setSpeaking(false);setVideo(null);setAvatarReady(false)};speechSynthesis.cancel();speechSynthesis.speak(u)}};
const ask=async text=>{text=text.trim();if(!text||busy)return;stopMike();setInput('');setBusy(true);setError('');setMessages(m=>[...m,{role:'user',text}]);try{const d=await fetchJson('/api/ask',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({message:text,history:messages.slice(-10)})},50000);setMessages(m=>[...m,{role:'mike',text:d.text}]);await speak(d.text)}catch(e){const msg=e.name==='AbortError'?'Mike is taking too long to respond. Try that again.':"I hit a snag on my end. Give me another shot.";setError(msg);setMessages(m=>[...m,{role:'mike',text:msg}])}finally{setBusy(false)}};
const listen=()=>{if(busy||speaking)return;const SR=window.SpeechRecognition||window.webkitSpeechRecognition;if(!SR){document.querySelector('#input')?.focus();return}recognitionRef.current?.abort();const r=new SR();recognitionRef.current=r;r.lang='en-US';r.interimResults=false;r.continuous=false;setError('');setListening(true);r.onresult=e=>{setListening(false);ask(e.results[0][0].transcript)};r.onerror=e=>{setListening(false);if(e.error!=='aborted'&&e.error!=='no-speech')setError('I couldn’t hear that. Try again.');document.querySelector('#input')?.focus()};r.onend=()=>setListening(false);r.start()};
useEffect(()=>{const key=e=>{if(e.key==='Escape'){recognitionRef.current?.abort();setListening(false);stopMike()}};window.addEventListener('keydown',key);return()=>{window.removeEventListener('keydown',key);recognitionRef.current?.abort();stopMike()}},[]);
return <main><header><div className="brand"><b>M</b><div><strong>MIKE AI</strong><small>DOER TOUGH</small></div></div><span className="status">● {listening?'MIKE IS LISTENING':speaking?'MIKE IS TALKING':busy?'MIKE IS THINKING':'MIKE IS HERE'}</span></header><section className="hero"><div><div className={'avatar '+(avatarReady?'avatar-ready':'')}>{!previewFailed&&<video className={video?'idle hidden':'idle'} src={PREVIEW} autoPlay muted loop playsInline preload="auto" onLoadedData={()=>{setPreviewFailed(false);setAvatarReady(true)}} onError={()=>setPreviewFailed(true)}/>}<video className={video?'talking':'talking hidden'} src={video||undefined} autoPlay muted playsInline onEnded={()=>{setVideo(null);setAvatarReady(false)}} onError={()=>{setVideo(null);setAvatarReady(false)}}/>{previewFailed&&!video&&<div className="avatar-fallback"><span>M</span><small>Mike AI</small></div>}<div className="halo"/></div><button className={'talk '+(listening?'active':'')} onClick={listen} disabled={busy||speaking}>{listening?<><Mic size={18}/>Listening…</>:<><Mic size={18}/>Talk to Mike <ArrowRight size={16}/></>}</button></div><div className="copy"><label>YOUR EVERYDAY COPILOT</label><h1>Meet Mike.<br/><span>Your everyday copilot.</span></h1><p>Talk it out. Think it through. Find the deal. Make the move. Mike helps you research, plan, negotiate, write, buy, sell, and figure out what to do next.</p><button className="radar" onClick={()=>ask('What am I missing?')} disabled={busy}><Lightbulb size={17}/> What am I missing?</button></div></section><section className="chat" aria-live="polite">{messages.map((m,i)=><div key={i} className={'bubble '+m.role}>{m.text}</div>)}{busy&&<div className="bubble mike">Give me a second. I'm thinking…</div>}</section>{error&&<div className="error" role="alert">{error}</div>}<form onSubmit={e=>{e.preventDefault();ask(input)}}><input id="input" value={input} onChange={e=>setInput(e.target.value)} placeholder="What's on your mind?" autoComplete="off"/><button disabled={!input.trim()||busy} aria-label="Send"><Send size={18}/></button><button type="button" className="read" aria-label={speaking?'Stop Mike':'Read latest response'} onClick={()=>{if(speaking)stopMike();else{const x=messages.at(-1);if(x?.role==='mike')speak(x.text)}}}>{speaking?<Square size={17}/>:<Volume2 size={18}/>}</button></form><p className="fine">Mike is a Doer Tough AI copilot. Current facts and changing information should be verified before important decisions.</p></main>};createRoot(document.getElementById('root')).render(<App/>);