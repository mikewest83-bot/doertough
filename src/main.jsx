import React,{useEffect,useRef,useState}from'react';import{createRoot}from'react-dom/client';import{Mic,Send,Volume2,ArrowRight,Lightbulb}from'lucide-react';import'./style.css';
const PREVIEW='/api/avatar-preview';
function App(){const[messages,setMessages]=useState([{role:'mike',text:"What's up? I'm Mike. Tell me what you're trying to figure out. We'll figure it out."}]),[input,setInput]=useState(''),[busy,setBusy]=useState(false),[speaking,setSpeaking]=useState(false),[video,setVideo]=useState(null),[previewFailed,setPreviewFailed]=useState(false),audioRef=useRef(null);
const speak=async text=>{setSpeaking(true);setVideo(null);try{const r=await fetch('/api/tts',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({text})}),d=await r.json();if(!r.ok)throw Error(d.error);const a=new Audio('data:audio/mpeg;base64,'+d.audioBase64);audioRef.current=a;a.onended=()=>setSpeaking(false);await a.play();try{const ar=await fetch('/api/avatar',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({audioBase64:d.audioBase64})}),ad=await ar.json();if(ar.ok&&ad.generationId)for(let i=0;i<40;i++){await new Promise(x=>setTimeout(x,1000));const sr=await fetch('/api/avatar/'+encodeURIComponent(ad.generationId)),sd=await sr.json();if(sd.status==='completed'&&sd.videoUrl){setVideo(sd.videoUrl);break}if(sd.status==='failed')break}}catch(e){console.warn('avatar unavailable',e)}}catch(e){const u=new SpeechSynthesisUtterance(text);u.lang='en-US';u.rate=1.02;u.pitch=.92;u.onend=()=>setSpeaking(false);speechSynthesis.cancel();speechSynthesis.speak(u)}};
const ask=async text=>{text=text.trim();if(!text||busy)return;setInput('');setBusy(true);setMessages(m=>[...m,{role:'user',text}]);try{const r=await fetch('/api/ask',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({message:text,history:messages.slice(-10)})}),d=await r.json();if(!r.ok)throw Error(d.error);setMessages(m=>[...m,{role:'mike',text:d.text}]);await speak(d.text)}catch(e){setMessages(m=>[...m,{role:'mike',text:"I hit a snag on my end. Give me another shot."}])}finally{setBusy(false)}};
const listen=()=>{const SR=window.SpeechRecognition||window.webkitSpeechRecognition;if(!SR){document.querySelector('#input')?.focus();return}const r=new SR();r.lang='en-US';r.interimResults=false;r.onresult=e=>ask(e.results[0][0].transcript);r.onerror=()=>document.querySelector('#input')?.focus();r.start()};useEffect(()=>()=>audioRef.current?.pause(),[]);
return <main><header><div className="brand"><b>M</b><div><strong>MIKE AI</strong><small>DOER TOUGH</small></div></div><span className="status">● {speaking?'MIKE IS TALKING':'MIKE IS HERE'}</span></header><section className="hero"><div><div className="avatar">{!previewFailed&&<video className={video?'idle hidden':'idle'} src={PREVIEW} autoPlay muted loop playsInline onError={()=>setPreviewFailed(true)}/>}<video className={video?'talking':'talking hidden'} src={video||undefined} autoPlay muted playsInline onEnded={()=>setVideo(null)} onError={()=>setVideo(null)}/>{previewFailed&&<div className="avatar-fallback"><span>M</span><small>Mike AI</small></div>}<div className="halo"/></div><button className="talk" onClick={listen}><Mic size={18}/> Talk to Mike <ArrowRight size={16}/></button></div><div className="copy"><label>YOUR EVERYDAY COPILOT</label><h1>Meet Mike.<br/><span>Your everyday copilot.</span></h1><p>Talk it out. Think it through. Find the deal. Make the move. Mike helps you research, plan, negotiate, write, buy, sell, and figure out what to do next.</p><button className="radar" onClick={()=>ask('What am I missing?')}><Lightbulb size={17}/> What am I missing?</button></div></section><section className="chat">{messages.map((m,i)=><div key={i} className={'bubble '+m.role}>{m.text}</div>)}{busy&&<div className="bubble mike">Give me a second. I'm thinking…</div>}</section><form onSubmit={e=>{e.preventDefault();ask(input)}}><input id="input" value={input} onChange={e=>setInput(e.target.value)} placeholder="What's on your mind?"/><button disabled={!input.trim()||busy}><Send size={18}/></button><button type="button" className="read" onClick={()=>{const x=messages.at(-1);if(x?.role==='mike')speak(x.text)}}><Volume2 size={18}/></button></form><p className="fine">Mike is a Doer Tough AI copilot. Current facts and changing information should be verified before important decisions.</p></main>};createRoot(document.getElementById('root')).render(<App/>);