// Mike AI pricing override disabled.
// Pricing is intentionally controlled by the main application until launch pricing
// is finalized. The previous whole-document MutationObserver could create a
// mutation -> patch -> mutation loop and lock the browser on the homepage.
export {};
